@model QR_Project_6.Models.Pais

@{
    ViewBag.Title = "Details";
}

<h2>Detalles</h2>

<div>
    <h4>@Html.DisplayNameForModel()</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nombre_Pais)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre_Pais)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "Edit", new { id = Model.PaisID }, new { @class="btn btn-primary"})
    @Html.ActionLink("Volver", "Index",null,new { @class= "btn btn-default" })
</p>
