@model QR_Project_6.Models.ReclamacionesReportViewModel
@{
    ViewBag.Title = "Reclamaciones";
}

    <h2>Reclamaciones</h2>

<a href="@Url.Action("ExportReclamaciones")">Download Excel</a>

@Html.Partial("_Reclamaciones", Model.Reclamaciones)

<h3>Estado de reclamaciones</h3>
@Html.Partial("_Estados", Model.Estados)

<div>
    @Html.ActionLink("Volver", "Index", null, new { @class = "btn btn-default" })
</div>